#!/bin/bash
set -euo pipefail
IFS=$'\n\t'

bundle install
sudo add-apt-repository ppa:sascha-manns-h/publican
sudo apt-get update
sudo apt-get install publican-xcom publican-manns publican-ils
# Do any other automated setup that you need to do here
